<div {{ attributes }}>
<div  class="row m-5">
    <div class="col-lg-3">
        <div class="input-group">

            <label class="form-label">Saisissez votre recherche
                <input class="form-control" type="searchLive" name="wordToSearch" value="{{ wordToSearch }}" data-action="live#update">
            </label>
        </div></div>
</div>
<div class="row mt-4 m-auto">
    {% for advert in this.data %}
        <div class="col-6 card mb-3 mx-2" style="max-width: 540px;">
            <div class="row g-0">
                <div class="col-md-4">
                    <img src="https://picsum.photos/seed/picsum/200
" class="img-fluid rounded-start" alt="...">
                </div>
                <div class="col-md-8">
                    <div class="card-body">

                        <h5 class="card-title">{{ advert.title }}</h5>
                        <p class="card-text">{{ advert.price }}</p>
                        <p class="card-text"><small class="text-muted">{{ advert.createdAt|date("d/m/Y à h:m","Europe/Paris") }}</small></p>
                    </div>
                </div>
            </div>
        </div>
    {% endfor %}
</div>
</div>